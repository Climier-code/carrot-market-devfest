import styled from 'styled-components';

export const ProductDetailWrapper = styled.section`
  margin: 1.5rem 1rem 0;
  padding-bottom: 0.25rem;
  box-shadow: 0 1px 0 0 hsla(0, 0%, 100%, 0.05);
`;

export const ProductName = styled.p`
  font-size: 1.25rem;
  font-weight: 700;
  margin-bottom: 0.625rem;
`;

export const ProductArticleDetail = styled.p`
  font-size: 0.8125rem;
  color: #868b94;
  margin-bottom: 1rem;
`;

export const ProductInfo = styled.p`
  font-size: 1rem;
  line-height: 1.375rem;
  margin-bottom: 1.25rem;
`;

export const ProductArticleData = styled.p`
  font-size: 0.8125rem;
  color: #868b94;
  margin-bottom: 1rem;
`;
